name: CI/CD Pipeline

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]
  release:
    types: [created]

jobs:
  call-shared-workflow:
    uses: YOUR_GITHUB_USERNAME/.github/.github/workflows/reusable-maven-docker.yml@main
    secrets: inherit # Automatically passes all secrets without declaring them